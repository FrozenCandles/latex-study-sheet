\documentclass{article}
\pagestyle{empty}

\usepackage{ctex}

\input{../../defs.tex}

\geometry{paperheight=4cm,top=0.5cm}

\begin{document}

使用 \boxforcmd{\footnote{}} 命令可以为对应的位置添加脚注 \footnote{脚注上方会自动添加脚注线}，对应的位置会自动添加一个脚注标号。使用命令的可选参数可以修改脚注序号 \footnote[3]{例如，这个脚注命令的可选参数是 {\tt [3]}} 。

\end{document}